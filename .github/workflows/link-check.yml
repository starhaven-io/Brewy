name: Link Check

on:
  pull_request:
    paths:
      - "README.md"
      - "CONTRIBUTING.md"
      - "SECURITY.md"
      - "lychee.toml"
  schedule:
    - cron: "0 14 * * 1" # every Monday at 14:00 UTC
  workflow_dispatch:

permissions: {}

concurrency:
  group: "link-check-${{ github.ref }}"
  cancel-in-progress: true

jobs:
  lychee:
    name: Check links
    permissions:
      contents: read
    uses: starhaven-io/.github/.github/workflows/reusable-link-check.yml@a2ff1457a008d2fe895f044330469e38d6d97a6d # v2026.09.10.3
    with:
      args: "--config lychee.toml README.md CONTRIBUTING.md SECURITY.md"
      build-site: false
      site-directory: ""
      authenticated-github: true
